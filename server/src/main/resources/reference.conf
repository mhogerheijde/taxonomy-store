{

  server {

    port = 50051

    // Either file: or classpath: URLs are supported
    certificate = "classpath:/certificates/server.crt"
    privateKey = "classpath:/certificates/server.pem"

  }

}